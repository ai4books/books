# Clean fixture: placeholder env with empty values and no real secrets.
# Variable names mention KEY/SECRET/TOKEN to tempt a false positive; the
# scanner keys on the VALUE, so empty values produce zero findings.
AWS_ACCESS_KEY_ID=
STRIPE_SECRET_KEY=
GITHUB_TOKEN=
