// PLANTED FIXTURE. Fake Stripe key, structure broken so it is not a live key.
// The scanner should flag the prefix; it must report it redacted, never raw.
export const stripeKey = "sk_live_EXAMPLE_redacted_not_real";
export const publicUrl = "https://example.com";
