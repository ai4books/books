# PLANTED FIXTURE. Deliberately fake and structurally broken so real scanners
# and pre-commit hooks do not treat these as live keys. Named env.leaked (not
# .env.leaked) on purpose, so it is safe to commit. One secret-shaped value.
AWS_ACCESS_KEY_ID=AKIA_EXAMPLE_NOT_REAL
AWS_REGION=us-east-1
